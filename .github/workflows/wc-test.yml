name: Test workflow for wordcount

on:
  workflow_call:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: test
        run: |
          pwd
          sudo snap install tree
          sudo snap install htmlq
          tree
          scripts/docstats


